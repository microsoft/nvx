name: Package release
description: Build and upload a simple NVX binary release for one platform

inputs:
  platform:
    description: Platform suffix used in the package and artifact names
    required: true

runs:
  using: composite
  steps:
    - name: Package Linux release
      if: runner.os != 'Windows'
      shell: bash
      env:
        PLATFORM: ${{ inputs.platform }}
      run: |
        set -euo pipefail
        version="$(cat VERSION)"
        simple_package="nvx-${version}-${PLATFORM}-simple"
        python3 scripts/nvx.py package \
          --version "${version}" \
          --destination "dist/${simple_package}" \
          --transport simple \
          --binary-only
        python3 scripts/nvx.py archive-release \
          --bundle "dist/${simple_package}" \
          --output "dist/${simple_package}.tar.gz"

    - name: Package Windows release
      if: runner.os == 'Windows'
      shell: powershell
      env:
        PLATFORM: ${{ inputs.platform }}
      run: |
        $Version = (Get-Content VERSION -Raw).Trim()
        $SimplePackage = "nvx-$Version-$env:PLATFORM-simple"
        python scripts\nvx.py package `
          --version $Version `
          --destination "dist\$SimplePackage" `
          --transport simple `
          --binary-only
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
        python scripts\nvx.py archive-release `
          --bundle "dist\$SimplePackage" `
          --output "dist\$SimplePackage.zip"
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

    - name: Upload Linux release
      if: runner.os != 'Windows'
      uses: actions/upload-artifact@v4
      with:
        name: release-${{ inputs.platform }}
        path: dist/*-simple.tar.gz
        if-no-files-found: error
        compression-level: 0
        retention-days: 1

    - name: Upload Windows release
      if: runner.os == 'Windows'
      uses: actions/upload-artifact@v4
      with:
        name: release-${{ inputs.platform }}
        path: dist/*-simple.zip
        if-no-files-found: error
        compression-level: 0
        retention-days: 1
