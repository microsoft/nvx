#!/bin/sh
set -eu
@SMP_PROBE_PATH
IFS= read -r trigger
if [ "$trigger" != nvx-snapshot ]; then
  /sbin/nvx-exit 90
  exit 90
fi
@{DISPATCH_MARKER}/sbin/nvx-snapshot
@{POST_RESTORE_ACTION}echo @RESTORE_MARKER
